<div class="container">
  <div class="row justify-content-md-center">
    <div class="col col-lg-6">

      <div class="d-flex flex-column mt-4">
        {{!-- 選單 --}}
        <select class="form-select" aria-label="Default select example">
          <option selected>類別</option>
          <option value="家居物業">家居物業</option>
          <option value="交通出行">交通出行</option>
          <option value="休閒娛樂">休閒娛樂</option>
          <option value="餐飲食品">餐飲食品</option>
          <option value="其他">其他</option>
        </select>
      </div>

      <div class="d-flex justify-content-between mt-4">
        <p class="fs-2 mt-4 ms-4">總金額</p>
        <p class="text-warning me-4" style="font-size: 5rem;">25600</p>
      </div>

      <div class=" d-flex align-items-center" style="height: 100px;">
        <div class=" d-flex align-items-center bg-secondary bg-opacity-25" style="width: 400px;">
          <i class="fa-solid fa-house fa-3x ms-3" style="color: #6f8cbe;"></i>
          <div class="p-2 m-0">
            <p class="fs-4 m-0">午餐</p>
            <p class="m-0">2019/04/23</p>
          </div>
          <p class="fs-2 my-auto ms-auto me-2">60</p>
        </div>

        <div class="d-flex justify-content-end ms-auto">
          <a href='/restaurant/{{!-- {{restaurant.id}} --}}' class='btn btn-info me-2'>修改</a>
          <button type='button' class="btn btn-danger" data-bs-toggle="modal"
            data-bs-target="#deleteConfirmationModal-{{!-- {{restaurant.id}} --}}">刪除</button>
        </div>

        {{!-- <!-- 删除确认模态框 -->
        <div class="modal fade" id="deleteConfirmationModal-{{restaurant.id}}" tabindex="-1"
          aria-labelledby="deleteConfirmationModalLabel" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="deleteConfirmationModalLabel">確定刪除</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                確定要刪除餐廳 "{{restaurant.name}}" ?
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
                <form action="/restaurant/{{restaurant.id}}?_method=DELETE" method="POST" style="display:inline;">
                  <button type="submit" class="btn btn-danger">確定刪除</button>
                </form>
              </div>
            </div>
          </div>
        </div> --}}

      </div>

    </div>
  </div>
</div>